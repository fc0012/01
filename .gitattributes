# Set default behavior to automatically normalize line endings
* text=auto

# Shell scripts must use Unix line endings
*.sh text eol=lf

# Batch files must use Windows line endings
*.bat text eol=crlf
*.cmd text eol=crlf
